frontend:
  phases:
    preBuild:
      commands:
        - cd dormradar
        - npm ci
    build:
      commands:
        - npm run build
        - npm run export
        - ls -la
  artifacts:
    baseDirectory: dormradar/out
    files:
      - '**/*'